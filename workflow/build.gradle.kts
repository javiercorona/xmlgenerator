plugins {
    kotlin("jvm")
}

group = "com.example.mybigapp.workflow"
version = "1.0.0"

dependencies {
    implementation(kotlin("stdlib"))
    // later: add RxJava, kotlinx-coroutines, or JSON libs if needed
}
